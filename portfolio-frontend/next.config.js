// next.config.js

/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: ['shorturl.at', 'localhost', 'shopspark', 'almn.me'], // Replace with real domain if needed
  },
};

module.exports = nextConfig;
